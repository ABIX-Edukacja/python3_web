# wykonujemy pętlę
for e in range(5):
    # wewnątrz pętli stosujemy instrukcję warunkową
    if e % 2 == 0:
        # liczba parzysta
        print("* ? * ? *")
    else:
        # w innym przypadku
        print("? * ? * ?")
